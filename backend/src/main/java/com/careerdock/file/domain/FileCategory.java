package com.careerdock.file.domain;

/**
 * 파일 보관함의 분류. 프론트의 MaterialFileType과 대응한다.
 */
public enum FileCategory {
    PROFILE_PHOTO,
    TRANSCRIPT,
    GRADUATION_CERTIFICATE,
    CREDENTIAL_PROOF,
    CAREER_CERTIFICATE,
    PORTFOLIO,
    OTHER
}
