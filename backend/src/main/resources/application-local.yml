spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:careerdock}
    username: ${DB_USERNAME:careerdock}
    password: ${DB_PASSWORD:careerdock_dev_password}
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate
